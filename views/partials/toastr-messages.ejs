<script>
    const toastrMessages = {
        successTitle: "<%= alerts.successTitle %>",
        successMessage: "<%= alerts.successMessage %>",
        
        errorTitle: "<%= alerts.errorTitle %>",
        errorMessage: "<%= alerts.errorMessage %>",

        warningMessage: "<%= alerts.warningMessage %>"
    };
</script>